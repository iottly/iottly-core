# this code has lost its binding with the "{{ type }}" command
# hence it will never get executed anymore
# but it has been kept here for your convenience

# this is likely because you removed or edited the command
# in case you modified it you'll find a freshly generated "{{ type }}" snippet 

{{ body }}
